import styled from 'styled-components'

function Chat({ id, users }) {
  return (
    <Container>
      <p>Recipeint Email</p>
    </Container>
  )
}

export default Chat

const Container = styled.div``
